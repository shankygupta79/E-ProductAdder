git push -u origin master (In starting)

use (git init) in new folder

git remote add origin https://github.com/shankygupta79/WebD-NodeJS-CB

git config --global user.email "shankygupta79@gmail.com"
git config --global user.name "shankygupta79"



git add BIT.cpp (Adding file)
git commit -m "This is that " (Message )
git push (To cloud Git)

/**
 * @author Shubham Gupta
 * @email shankygupta79@gmail.com
 * @date 13-Feb-2019
 */

git fetch origin
git reset --hard origin/master

//These two are used to hard reset

Right click on any folder and click Git Bash Here

